include:
  - experiments/include/vae2d/fid.yaml
  - experiments/celeba/include/common.yaml

exp_params:
  plot:
    params:
      title: "${model}, CelebA, FID loss, Epoch ${epoch}"

logging_params:
  name: "CelebA_VAE_FID"