include:
  - experiments/include/vae2d/mse.yaml
  - experiments/celeba/include/common.yaml
  
exp_params:
  plot:
    params:
      title: "${model}, CelebA, MSE loss, Epoch ${epoch}"

logging_params:
  name: "CelebA_VAE_MSE"