model_params:
  dropout:
    grid_search: [0.1, 0.2, 0.3]

exp_params:
  optimizer:
    lr:
      grid_search: [0.0005, 0.001, 0.005, 0.01, 0.02]
  data:
    name: forrestgump
    training:
      label_offset:
      # Offset all labels this many seconds into the future
        grid_search: [0, 2, 4, 6]
