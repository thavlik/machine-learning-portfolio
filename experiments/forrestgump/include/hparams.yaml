exp_params:
  optimizer:
    lr:
      grid_search: [0.08, 0.01, 0.02, 0.025]
  data:
    training:
      offset:
      # Offset all labels this many seconds into the future
        grid_search: [0, 2, 4, 6]
