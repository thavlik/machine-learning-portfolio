exp_params:
  optimizer:
    lr:
      grid_search: [0.001, 0.005, 0.01]
  data:
    training:
      offset:
      # Offset all labels this many seconds into the future
        grid_search: [0, 2, 4, 6]
