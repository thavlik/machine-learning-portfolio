include:
  - experiments/abstract/vae2d/mse.yaml
  - experiments/celeba/common.yaml
  
exp_params:
  plot:
    params:
      title: "${model}, CelebA, MSE loss, Epoch ${epoch}"

logging_params:
  name: "CelebA_VAE_MSE"