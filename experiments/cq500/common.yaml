model_params:
  pooling: max
  
exp_params:
  data:
    name: cq500
    loader:
      num_workers: 0
      drop_last: true
    training:
      dir: E:/cq500
    validation:
      #FIXME it will look like it's crazy effective this way lol
      dir: E:/cq500
  plot:
    fn: dcm
    params:
      title: "${model}, CQ500 Intracranial Hemorrhage CT Slices, Epoch ${epoch}"
      scaling: 2.0
      suptitle:
        y: 0.78
