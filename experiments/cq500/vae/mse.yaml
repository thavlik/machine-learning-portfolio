include:
  - experiments/abstract/vae2d/mse.yaml
  - experiments/cq500/common.yaml

exp_params:
  plot:
    params:
      title: "${model}, CQ500, MSE loss, Epoch ${epoch}"

logging_params:
  name: "CQ500_VAE_MSE"