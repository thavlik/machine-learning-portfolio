exp_params:
  data:
    name: reference
    training:
      name: MNIST
      params:
        root: E:/mnist
        download: true
  batch_size: 64
  plot:
    params:
      title: "${model}, MNIST (Epoch ${epoch})"
