exp_params:
  data:
    name: reference
    training:
      name: MNIST
      params:
        root: E:/
        download: true
  batch_size: 64
  plot:
    params:
      title: "${model}, MNIST, Epoch ${epoch}"
