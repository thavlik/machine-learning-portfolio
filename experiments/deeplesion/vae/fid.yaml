include:
  - experiments/abstract/vae2d/fid.yaml
  - experiments/deeplesion/common.yaml

exp_params:
  plot:
    params:
      title: "${model}, DeepLesion, FID loss, Epoch ${epoch}"

logging_params:
  name: "DeepLesion_VAE_FID"