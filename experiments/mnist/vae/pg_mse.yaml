include:
  - experiments/abstract/vae2d/mse.yaml
  - experiments/mnist/common.yaml

exp_params:
  plot:
    params:
      title: "${model}, MNIST, Progressive Growing, MSE loss, Epoch ${epoch}"

logging_params:
  name: "MNIST_VAE_ProgressiveGrowingMSE"
