include:
  - experiments/abstract/vae2d/fid.yaml
  - experiments/mnist/include/common.yaml

exp_params:
  plot:
    params:
      title: "${model}, MNIST, FID loss, Epoch ${epoch}"

logging_params:
  name: "MNIST_VAE_FID"
